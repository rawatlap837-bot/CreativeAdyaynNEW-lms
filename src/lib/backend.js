export { auth } from "./auth";
// Reference roots consumed by the relational repository and storage helpers.
export const db = Object.freeze({ path: [] });
export const storage = Object.freeze({ bucket: "lms-media" });
